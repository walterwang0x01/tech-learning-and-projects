# 架构与性能面试题
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

## 1. MVVM vs MVI

```kotlin
// MVVM: ViewModel 暴露多个 State/LiveData，View 分别观察
class MvvmViewModel : ViewModel() {
    val users = MutableStateFlow<List<User>>(emptyList())
    val isLoading = MutableStateFlow(false)
    val error = MutableStateFlow<String?>(null)
    // 问题：多个状态可能不一致
}

// MVI: 单一 State + 单向数据流
class MviViewModel : ViewModel() {
    private val _state = MutableStateFlow(UiState())
    val state: StateFlow<UiState> = _state.asStateFlow()

    fun onIntent(intent: UserIntent) {
        // Intent → Reducer → State → UI
        // 优点：状态一致性，可预测，易测试
        // 缺点：模板代码多，简单场景过度设计
    }
}

data class UiState(
    val users: List<User> = emptyList(),
    val isLoading: Boolean = false,
    val error: String? = null
)

// 选择建议：
// 简单页面 → MVVM
// 复杂交互/状态多 → MVI
// 团队统一 → 选一种坚持
```

## 2. Hilt vs Koin

```kotlin
// Hilt（编译时）
// ✅ 编译时检查依赖图，错误提前发现
// ✅ Google 官方支持，与 Jetpack 深度集成
// ❌ 编译时间增加，注解处理器
// ❌ 学习曲线较高

@HiltViewModel
class UserViewModel @Inject constructor(
    private val repository: UserRepository
) : ViewModel()

// Koin（运行时）
// ✅ DSL 简洁，上手快
// ✅ 纯 Kotlin，无注解处理
// ❌ 运行时解析，错误延迟到运行时
// ❌ 大型项目性能略差

val viewModel: UserViewModel by viewModel()

// 选择建议：
// 大型项目/团队 → Hilt（编译时安全）
// 小型项目/快速原型 → Koin（简洁高效）
// KMM 项目 → Koin（跨平台支持）
```

## 3. 内存泄漏检测与解决

```kotlin
// 常见泄漏场景及解决方案

// 1. 匿名内部类持有外部引用
// ❌ Handler/Runnable 持有 Activity
// ✅ 使用 lifecycleScope 或 WeakReference

// 2. 单例持有 Activity Context
// ❌ Singleton.init(activity)
// ✅ Singleton.init(activity.applicationContext)

// 3. 未注销监听器
// ❌ 注册了 BroadcastReceiver 但未 unregister
// ✅ 在 onStop/onDestroy 中注销

// 4. Coroutine 泄漏
// ❌ GlobalScope.launch { /* 持有 Activity 引用 */ }
// ✅ viewModelScope.launch / lifecycleScope.launch

// 5. Fragment ViewBinding 泄漏
// ❌ private val binding = FragmentBinding.inflate(...)
// ✅ onDestroyView 中 _binding = null

// 检测工具
// LeakCanary: debug 自动检测
// Android Profiler: Memory → Dump Heap
// StrictMode: 检测主线程 IO
```

## 4. Compose 重组优化

```kotlin
// 重组触发条件：State 变化时，读取该 State 的 Composable 重组

// 1. 稳定性标注
@Immutable  // 所有属性不可变
data class UserUiModel(val id: String, val name: String)

@Stable  // 可变但变化可被 Compose 追踪
class CounterState {
    var count by mutableStateOf(0)
}

// 2. 避免不稳定参数
// ❌ 每次重组创建新 List
@Composable
fun UserList() {
    val users = listOf(User("1", "张三"))  // 每次新对象
    ItemList(users)
}

// ✅ 使用 remember
@Composable
fun UserList() {
    val users = remember { listOf(User("1", "张三")) }
    ItemList(users)
}

// 3. key 优化 LazyColumn
LazyColumn {
    items(users, key = { it.id }) { user ->
        UserItem(user)  // key 帮助识别项目，减少重组
    }
}

// 4. derivedStateOf 减少重组频率
val showButton by remember {
    derivedStateOf { listState.firstVisibleItemIndex > 5 }
}

// 5. 检查重组次数
// Layout Inspector → Compose → Recomposition Counts
// Compose Compiler Metrics 分析稳定性
```

## 5. 启动优化策略

```kotlin
// 冷启动优化目标：< 500ms

// 1. Application.onCreate 优化
// - 必要初始化放主线程
// - 非必要初始化异步执行
// - 使用 App Startup 管理初始化顺序

// 2. 首屏优化
// - SplashScreen API 替代自定义启动页
// - 延迟加载非首屏内容
// - 使用 ViewStub / LazyColumn

// 3. Baseline Profiles
// - 预编译热路径代码
// - 首次启动速度提升 30%+

// 4. 测量工具
// adb shell am start-activity -W com.example/.MainActivity
// Macrobenchmark 自动化测量
@RunWith(AndroidJUnit4::class)
class StartupBenchmark {
    @get:Rule
    val rule = MacrobenchmarkRule()

    @Test
    fun startupCompilation() = rule.measureRepeated(
        packageName = "com.example.app",
        metrics = listOf(StartupTimingMetric()),
        iterations = 5,
        startupMode = StartupMode.COLD
    ) {
        pressHome()
        startActivityAndWait()
    }
}
```

## 6. 进程保活（了解即可）

```kotlin
// Android 系统会按优先级回收进程
// 前台进程 > 可见进程 > 服务进程 > 缓存进程

// 合规保活方式：
// 1. 前台 Service（用户可见通知）
// 2. WorkManager 定期任务
// 3. FCM 推送唤醒
// 4. JobScheduler

// 不推荐的方式（可能被系统限制）：
// 双进程守护、1px Activity、播放无声音频
```

> 🔄 更新于 2026-05-02

<!-- version-check: Android 16 API 36, Compose 1.11, Hilt 2.57, checked 2026-05-02 -->

## 7. 2026 年架构与性能面试新题

### 7.1 Compose 重组优化进阶

```kotlin
// Q: Compose 1.11 测试 API v2 有什么变化？
// A: 默认使用 StandardTestDispatcher（替代 UnconfinedTestDispatcher）：
// - 协程不再立即执行，而是排队等待虚拟时钟推进
// - 更接近生产环境行为，能暴露竞态条件
// - 测试更稳定，减少 flaky test
// 迁移：需要在测试中显式推进时钟（advanceUntilIdle）

// Q: SharedElement 调试工具怎么用？
// A: Compose 1.11 新增 LookaheadAnimationVisualDebugging：
@Composable
fun DebugTransition() {
    LookaheadAnimationVisualDebugging(
        overlayColor = Color(0x4AE91E63),
        isEnabled = true,
        unmatchedElementColor = Color.Red,
    ) {
        SharedTransitionLayout {
            // 可视化目标边界、动画轨迹、匹配数量
        }
    }
}

// Q: 如何检测 Compose 性能问题？
// A: 2026 年推荐工具链：
// 1. Vkompose（编译时检测不稳定参数和重组问题）
// 2. Layout Inspector → Recomposition Counts
// 3. Compose Compiler Metrics（分析稳定性报告）
// 4. Macrobenchmark（自动化性能基准测试）
```

### 7.2 Android 16 适配面试题

```kotlin
// Q: Android 16 强制自适应布局意味着什么？
// A: targetSdk 36 的 App：
// - 系统可覆盖 screenOrientation、resizeableActivity、maxAspectRatio
// - 折叠屏/平板上不能再锁定竖屏
// - 必须测试多窗口、分屏、不同宽高比
// 应对：使用 WindowSizeClass 做响应式布局

// Q: Baseline Profiles 在 2026 年有什么变化？
// A: 仍然是启动优化的核心手段：
// - 预编译热路径代码，首次启动速度提升 30%+
// - AGP 9.0 简化了 Baseline Profile 生成流程
// - 配合 Macrobenchmark 自动化测量冷启动时间
```

### 7.3 Android 17 大屏适配与 Material 3 Adaptive

<!-- version-check: Android 17 API 37 (Beta 4 April 2026, stable June 2026), Compose Multiplatform 1.11, Material 3 Adaptive 1.2.0, checked 2026-05-22 -->

> 🔄 更新于 2026-05-22

```kotlin
// Q: Android 17 大屏强制适配为什么会影响所有 App，包括手机端 App？
// A: 三个层面的连锁影响：
// 1. Play Console 提交校验：targetSdk 37 不通过自适应测试将被拒绝
// 2. 折叠屏用户基数：到 2026 Q3，全球折叠屏出货量预计破 3000 万台
// 3. ChromeOS 集成：Android App 在 Chromebook 上不再是次等公民，强制要求支持窗口缩放
// 来源：https://android-developers.googleblog.com/2026/02/prepare-your-app-for-resizability-and.html

// Q: 在 Compose 中如何快速适配 Android 17 大屏？
// A: 使用 Material 3 Adaptive 1.2.0（2026-05-06 stable）：
// - NavigableListDetailPaneScaffold 自动处理 list-detail 双栏
// - 根据 WindowSizeClass 自动切换单栏/双栏/三栏
// - 配合 Compose BOM 2026.05.00

dependencies {
    implementation(platform("androidx.compose:compose-bom:2026.05.00"))
    implementation("androidx.compose.material3.adaptive:adaptive-navigation:1.2.0")
}

@Composable
fun AdaptiveScreen() {
    val navigator = rememberListDetailPaneScaffoldNavigator<Item>()
    
    NavigableListDetailPaneScaffold(
        navigator = navigator,
        listPane = { ItemList(onItemClick = { navigator.navigateTo(ListDetailPaneScaffoldRole.Detail, it) }) },
        detailPane = { 
            navigator.currentDestination?.contentKey?.let { ItemDetail(it) }
        }
    )
}

// Q: Compose Multiplatform 1.11 在跨平台项目中的关键升级是什么？
// A: 三个生产级改进：
// 1. iOS 原生文本输入（实验性）：UIView-based，提供 Autofill/Translate/Search 系统菜单
// 2. iOS Stable 自 1.8.0 起持续迭代，1.11 可作为 KMP 项目的稳定基线
// 3. 与 Kotlin 2.4 RC 协同发布，编译速度持续优化
// 来源：https://blog.jetbrains.com/kotlin/2026/05/compose-multiplatform-1-11-0/

// Q: 2026 年面试常考的 Android 性能优化新指标是什么？
// A: 除经典的冷启动、内存、卡顿外，新增三个指标：
// 1. P99 INP (Interaction to Next Paint)：从用户操作到下一帧的时延，对应 Compose 重组耗时
// 2. JNI 跨界开销：Compose Multiplatform 项目中，commonMain ↔ platformMain 的桥接成本
// 3. Baseline Profile 命中率：通过 dumpsys android.os.PrebuiltModeStatus 监控

// Q: Hilt 1.4.0-beta01 改进的 rememberHiltViewModelFactory 解决了什么问题？
// A: 修复了"在嵌套 ViewModelStoreOwner 中获取 Hilt ViewModel 必须显式传 owner"的痛点：
// - API 简化，移除 ViewModelStoreOwner 参数
// - 通过 delegateFactory 自定义工厂逻辑
// - 配合 rememberViewModelStoreOwner 在多层 NavGraph 场景表现更好
// - 来源：https://developer.android.com/jetpack/androidx/releases/hilt
```
