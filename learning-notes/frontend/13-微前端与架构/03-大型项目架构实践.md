# 大型项目架构实践
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

## 1. 目录结构

```
src/
├── app/                    # 应用层（路由、布局、Provider）
│   ├── routes.tsx
│   ├── App.tsx
│   └── providers.tsx
├── pages/                  # 页面组件（按路由组织）
│   ├── home/
│   ├── dashboard/
│   └── settings/
├── features/               # 功能模块（按业务领域）
│   ├── auth/
│   │   ├── components/
│   │   ├── hooks/
│   │   ├── api.ts
│   │   ├── store.ts
│   │   └── types.ts
│   └── user/
├── shared/                 # 共享资源
│   ├── components/         # 通用UI组件
│   ├── hooks/              # 通用Hooks
│   ├── utils/              # 工具函数
│   ├── types/              # 全局类型
│   └── constants/          # 常量
├── api/                    # API 层
│   ├── client.ts           # HTTP 客户端
│   ├── interceptors.ts     # 请求/响应拦截
│   └── endpoints/          # API 端点
└── styles/                 # 全局样式
```

## 2. API 层封装

```typescript
// api/client.ts
const client = axios.create({
  baseURL: import.meta.env.VITE_API_URL,
  timeout: 10000,
});

client.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

client.interceptors.response.use(
  (res) => res.data,
  (err) => {
    if (err.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(err);
  },
);

// api/endpoints/user.ts
export const userApi = {
  getList: (params) => client.get('/users', { params }),
  getById: (id) => client.get(`/users/${id}`),
  create: (data) => client.post('/users', data),
  update: (id, data) => client.put(`/users/${id}`, data),
  delete: (id) => client.delete(`/users/${id}`),
};
```

## 3. 错误处理体系

```typescript
// 全局错误边界（React 18+）
// 注：React 19+ 推荐使用 react-error-boundary 库，提供 hooks 风格 API
class ErrorBoundary extends React.Component {
  state = { hasError: false, error: null };

  static getDerivedStateFromError(error) {
    return { hasError: true, error };
  }

  componentDidCatch(error, errorInfo) {
    reportError(error, errorInfo); // 上报错误
  }

  render() {
    if (this.state.hasError) return <ErrorFallback error={this.state.error} />;
    return this.props.children;
  }
}

// 全局未捕获错误
window.addEventListener('unhandledrejection', (e) => {
  reportError(e.reason);
});
```

## 4. 权限控制

```typescript
// 路由级别权限
const routes = [
  { path: '/dashboard', element: <Dashboard />, roles: ['admin', 'user'] },
  { path: '/admin', element: <Admin />, roles: ['admin'] },
];

function ProtectedRoute({ roles, children }) {
  const { user } = useAuth();
  if (!user) return <Navigate to="/login" />;
  if (roles && !roles.includes(user.role)) return <Navigate to="/403" />;
  return children;
}

// 组件级别权限
function Permission({ roles, children }) {
  const { user } = useAuth();
  if (!roles.includes(user?.role)) return null;
  return children;
}

<Permission roles={['admin']}>
  <DeleteButton />
</Permission>
```

## 5. 国际化（i18n）

<!-- version-check: react-i18next 16.x, i18next 26.x, checked 2026-05-15 -->

```typescript
// react-i18next（v16+ 兼容 React 19、Next.js 16 App Router）
import i18n from 'i18next';
import { initReactI18next, useTranslation } from 'react-i18next';

i18n.use(initReactI18next).init({
  resources: {
    zh: { translation: { welcome: '欢迎', greeting: '你好，{{name}}' } },
    en: { translation: { welcome: 'Welcome', greeting: 'Hello, {{name}}' } },
  },
  lng: 'zh',
  fallbackLng: 'en',
});

function App() {
  const { t, i18n } = useTranslation();
  return (
    <div>
      <h1>{t('welcome')}</h1>
      <p>{t('greeting', { name: '张三' })}</p>
      <button onClick={() => i18n.changeLanguage('en')}>English</button>
    </div>
  );
}
```
