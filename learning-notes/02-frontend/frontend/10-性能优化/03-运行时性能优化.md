# 运行时性能优化
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

## 1. 防抖与节流

```javascript
// 防抖（debounce）：最后一次触发后延迟执行
function debounce(fn, delay = 300) {
  let timer;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}
// 场景：搜索输入、窗口 resize

// 节流（throttle）：固定时间间隔内只执行一次
function throttle(fn, interval = 100) {
  let lastTime = 0;
  return function(...args) {
    const now = Date.now();
    if (now - lastTime >= interval) {
      lastTime = now;
      fn.apply(this, args);
    }
  };
}
// 场景：滚动事件、鼠标移动
```

## 2. 虚拟列表

```
只渲染可视区域内的元素，大幅减少 DOM 节点数量

┌─────────────────┐
│  缓冲区（上方）   │  ← 少量预渲染
├─────────────────┤
│                 │
│   可视区域       │  ← 实际渲染的元素
│                 │
├─────────────────┤
│  缓冲区（下方）   │  ← 少量预渲染
└─────────────────┘

库推荐：
- React: @tanstack/react-virtual, react-virtuoso
- Vue: vue-virtual-scroller
```

## 3. Web Worker

```javascript
// 将 CPU 密集型任务移到 Worker 线程
const worker = new Worker(new URL('./worker.js', import.meta.url));

worker.postMessage({ type: 'sort', data: largeArray });
worker.onmessage = (e) => {
  console.log('排序完成:', e.data);
};

// worker.js
self.onmessage = (e) => {
  if (e.data.type === 'sort') {
    const sorted = e.data.data.sort((a, b) => a - b);
    self.postMessage(sorted);
  }
};
```

## 4. requestAnimationFrame

```javascript
// ✅ 使用 rAF 做动画（与屏幕刷新率同步）
function smoothScroll(target) {
  const start = window.scrollY;
  const distance = target - start;
  const duration = 500;
  let startTime;

  function step(timestamp) {
    if (!startTime) startTime = timestamp;
    const progress = Math.min((timestamp - startTime) / duration, 1);
    const eased = 1 - Math.pow(1 - progress, 3); // ease-out
    window.scrollTo(0, start + distance * eased);
    if (progress < 1) requestAnimationFrame(step);
  }
  requestAnimationFrame(step);
}
```

## 5. 内存泄漏排查

```javascript
// 常见内存泄漏场景
// 1. 事件监听器未移除
useEffect(() => {
  const handler = () => {};
  window.addEventListener('resize', handler);
  return () => window.removeEventListener('resize', handler); // ✅ 清理
}, []);

// 2. 定时器未清理
useEffect(() => {
  const timer = setInterval(() => {}, 1000);
  return () => clearInterval(timer); // ✅ 清理
}, []);

// 3. 闭包引用大对象
// 4. 全局变量累积
// 5. 分离的 DOM 节点

// 排查工具：Chrome DevTools → Memory → Heap Snapshot
```
