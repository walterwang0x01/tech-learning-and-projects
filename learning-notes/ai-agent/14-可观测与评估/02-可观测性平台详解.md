# 可观测性平台详解
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

## 1. 平台全景

```
┌─────────────────────────────────────────────────────┐
│            AI Agent 可观测性平台                       │
├──────────┬──────────┬──────────┬───────────────────┤
│LangSmith │LangFuse  │Arize    │AgentOps           │
│LangChain │开源自托管 │Phoenix  │CrewAI 原生         │
│原生       │多框架    │开源     │                    │
├──────────┼──────────┼──────────┼───────────────────┤
│Helicone  │Braintrust│          │                    │
│代理模式   │评估优先  │          │                    │
│零代码     │数据集    │          │                    │
└──────────┴──────────┴──────────┴───────────────────┘
```

## 2. LangSmith

> 🔄 更新于 2026-05-14

<!-- version-check: LangSmith Engine + SmithDB + Context Hub + Sandboxes GA + LLM Gateway (Interrupt 2026), checked 2026-05-14 -->

LangChain 官方可观测性平台，与 LangGraph 深度集成。2026-03 Agent Builder 更名为 **LangSmith Fleet**，从可观测性工具升级为企业级 Agent 管理平台。

```
LangSmith Fleet（2026-03 更名）新增能力：
├─ Agent 身份管理：凭证模型控制 Agent 代表谁行动
├─ Agent 共享与权限：编辑/运行/共享的细粒度权限
├─ 人工审批：Agent 操作的 Human-in-the-Loop 审批流
├─ 审计追踪：完整的 Agent 操作审计日志
├─ 中央 Chat Agent：一个 Agent 访问工作区所有工具
├─ 对话转 Agent：一键将聊天对话转为可复用 Agent
├─ 定时报告：Insights Agent 按日/周/自定义 cron 自动运行
├─ 文件上传：Agent 支持文件上传处理
├─ 工具注册表：集中管理所有可用工具
└─ 自托管 v0.13：扩展与云版本的功能对等
```

来源：[LangChain Blog: Introducing LangSmith Fleet](https://blog.langchain.com/introducing-langsmith-fleet/)（Content was rephrased for compliance with licensing restrictions）

> 🔄 更新于 2026-05-14

**Interrupt 2026 大会发布（2026-05-13）— LangSmith 平台级升级**：

```
Interrupt 2026 Day 1 发布（2026-05-13）：
├─ LangSmith Engine：自动化故障诊断
│   ├─ 持续聚类生产失败为命名 Issue
│   ├─ 对照代码诊断根因
│   ├─ 自动起草 PR 和评估器
│   └─ 每个解决的 Issue 增强 eval 套件
├─ SmithDB：专用分布式数据库
│   ├─ Rust + Apache DataFusion + Vortex 构建
│   ├─ Trace 树加载 P50 92ms / P99 595ms
│   ├─ 全文搜索 P50 400ms / P99 870ms
│   ├─ Run 过滤 P50 82ms / P99 434ms
│   ├─ 核心体验提速最高 12-15x
│   ├─ 对象存储后端，无状态服务，水平扩展
│   └─ 100% US Cloud 流量已迁移
├─ Context Hub：Agent 上下文版本管理
│   ├─ 集中存储、版本化、协作管理 Agent 上下文
│   ├─ 标签部署（staging → production）
│   ├─ 技能/策略/记忆的版本化 bundle
│   └─ 环境感知的指令和工具管理
├─ LangSmith Sandboxes GA：安全代码执行
│   ├─ 硬件虚拟化 microVM（非容器）
│   ├─ 完全内核隔离
│   └─ 安全运行不可信的模型生成代码
└─ LLM Gateway：运行时治理
    ├─ 位于 Agent 和 LLM 提供商之间
    ├─ 强制执行花费限制
    ├─ 请求到达模型前 PII 脱敏
    └─ 策略违规作为可追踪事件出现在 LangSmith 中
```

来源：[LangSmith Engine](https://www.langchain.com/blog/introducing-langsmith-engine)、[SmithDB](https://www.langchain.com/blog/introducing-smithdb)、[Context Hub](https://www.langchain.com/blog/introducing-context-hub)、[Sandboxes GA](https://www.langchain.com/blog/langsmith-sandboxes-generally-available)、[LLM Gateway](https://www.langchain.com/blog/introducing-llm-gateway)（Content was rephrased for compliance with licensing restrictions）

```python
# 零配置集成（设置环境变量即可）
import os
os.environ["LANGSMITH_API_KEY"] = "lsv2_xxx"
os.environ["LANGSMITH_TRACING"] = "true"
os.environ["LANGSMITH_PROJECT"] = "my-agent"

# LangGraph Agent 自动追踪
from langgraph.prebuilt import create_react_agent
from langchain_openai import ChatOpenAI

agent = create_react_agent(ChatOpenAI(model="gpt-5.2"), tools=[...])
result = agent.invoke({"messages": [("user", "你好")]})
# → LangSmith 自动记录完整 trace

# 手动追踪（非 LangChain 代码）
from langsmith import traceable

@traceable(name="custom_agent_step")
def my_agent_step(query: str) -> str:
    response = client.chat.completions.create(
        model="gpt-5.2",
        messages=[{"role": "user", "content": query}],
    )
    return response.choices[0].message.content

# 在线评估
from langsmith import Client

ls_client = Client()
ls_client.create_feedback(
    run_id="run-xxx",
    key="correctness",
    score=0.9,
    comment="回答准确",
)
```

```
LangSmith 核心功能：
├─ Trace 追踪：完整调用链可视化
├─ Prompt Hub：提示词版本管理
├─ 在线评估：对生产 trace 打分
├─ 数据集管理：构建评估数据集
├─ A/B 测试：对比不同配置效果
└─ 近零开销：<1ms 额外延迟
```

## 3. LangFuse

> 🔄 更新于 2026-05-13

<!-- version-check: LangFuse SDK v4 (2026-03), checked 2026-05-13 -->

开源可观测性平台，支持多框架，可自托管。2026-03 发布 SDK v4，重写为 observations-first 架构，查询性能提升一个数量级。`@observe()` 装饰器 API 在 v4 中仍然有效。

```python
# pip install langfuse
from langfuse import Langfuse
from langfuse.decorators import observe, langfuse_context

langfuse = Langfuse(
    public_key="pk-xxx",
    secret_key="sk-xxx",
    host="https://cloud.langfuse.com",  # 或自托管地址
)

# 装饰器方式追踪
@observe()
def agent_pipeline(query: str) -> str:
    # 自动追踪函数调用
    research = research_step(query)
    answer = generate_answer(research)
    return answer

@observe()
def research_step(query: str) -> str:
    # 记录 LLM 调用
    langfuse_context.update_current_observation(
        input=query,
        metadata={"step": "research"},
    )
    return "调研结果..."

# OpenAI 集成（自动追踪所有调用）
from langfuse.openai import openai
client = openai.OpenAI()
response = client.chat.completions.create(
    model="gpt-5.2",
    messages=[{"role": "user", "content": "你好"}],
)
# → 自动记录到 LangFuse

# 成本追踪
# LangFuse 自动计算每次调用的 Token 成本
# Dashboard 展示：日/周/月成本趋势
```

```bash
# 自托管部署（Docker）
docker compose up -d
# 访问 http://localhost:3000
```

## 4. Arize Phoenix

开源可观测性平台，专注嵌入分析和漂移检测。

```python
# pip install arize-phoenix
import phoenix as px

# 启动 Phoenix 服务
session = px.launch_app()
# → http://localhost:6006

# OpenAI 集成
from phoenix.otel import register
from openinference.instrumentation.openai import OpenAIInstrumentor

tracer_provider = register(project_name="my-agent")
OpenAIInstrumentor().instrument(tracer_provider=tracer_provider)

# 所有 OpenAI 调用自动追踪
from openai import OpenAI
client = OpenAI()
response = client.chat.completions.create(
    model="gpt-5.2",
    messages=[{"role": "user", "content": "你好"}],
)

# LangChain 集成
from openinference.instrumentation.langchain import LangChainInstrumentor
LangChainInstrumentor().instrument(tracer_provider=tracer_provider)
```

```
Arize Phoenix 特色功能：
├─ 嵌入聚类：可视化 embedding 分布
├─ 漂移检测：发现数据分布变化
├─ LLM 评估：内置评估器（幻觉、相关性等）
├─ RAG 分析：检索质量评估
└─ 完全开源：Apache 2.0
```

## 5. AgentOps

专为 Agent 设计的可观测性平台，CrewAI 原生集成。

```python
import agentops

# 初始化（自动追踪）
agentops.init(api_key="ao-xxx")

# CrewAI 自动集成
from crewai import Agent, Task, Crew

researcher = Agent(role="研究员", goal="调研", llm="gpt-5.2")
task = Task(description="调研 AI Agent", agent=researcher)
crew = Crew(agents=[researcher], tasks=[task])
result = crew.kickoff()
# → AgentOps 自动记录完整 Agent 会话

# 手动记录事件
agentops.record(agentops.ActionEvent(action_type="search", params={"query": "AI"}))

# 结束会话
agentops.end_session("Success")
```

```
AgentOps 特色：
├─ Session Replay：回放 Agent 完整执行过程
├─ Agent Analytics：Agent 级别的统计分析
├─ CrewAI 原生：零配置集成
├─ 成本追踪：按 Agent/Session 统计
└─ 错误追踪：自动捕获异常
```

## 6. Helicone

代理模式可观测性，零代码集成。

```python
from openai import OpenAI

# 只需修改 base_url，零代码集成
client = OpenAI(
    base_url="https://oai.helicone.ai/v1",
    default_headers={
        "Helicone-Auth": "Bearer hl-xxx",
        "Helicone-User-Id": "alice",
        "Helicone-Session-Id": "session-001",
    },
)

# 所有请求自动经过 Helicone 代理记录
response = client.chat.completions.create(
    model="gpt-5.2",
    messages=[{"role": "user", "content": "你好"}],
)

# 启用缓存（相同请求直接返回缓存）
client_cached = OpenAI(
    base_url="https://oai.helicone.ai/v1",
    default_headers={
        "Helicone-Auth": "Bearer hl-xxx",
        "Helicone-Cache-Enabled": "true",
    },
)
```

## 7. Braintrust

评估优先的可观测性平台。

```python
from braintrust import Eval, init_logger

# 评估驱动开发
await Eval(
    "my-agent",
    data=lambda: [
        {"input": "什么是 RAG？", "expected": "检索增强生成"},
        {"input": "LangGraph 是什么？", "expected": "工作流编排框架"},
    ],
    task=lambda input: my_agent(input),
    scores=[
        lambda output, expected: {
            "name": "correctness",
            "score": 1.0 if expected.lower() in output.lower() else 0.0,
        },
    ],
)

# 生产日志
logger = init_logger(project="my-agent")
logger.log(input="用户问题", output="Agent回答", scores={"quality": 0.9})
```

## 8. 功能对比

| 功能           | LangSmith | LangFuse  | Phoenix   | AgentOps  | Helicone  | Braintrust|
|---------------|-----------|-----------|-----------|-----------|-----------|-----------|
| Trace 追踪    | ✅        | ✅        | ✅        | ✅        | ✅        | ✅        |
| 成本追踪       | ✅        | ✅        | ✅        | ✅        | ✅        | ✅        |
| 评估框架       | ✅        | ✅        | ✅        | ❌        | ❌        | ✅ 核心   |
| Prompt 管理   | ✅ Hub    | ✅        | ❌        | ❌        | ✅        | ✅        |
| 嵌入分析       | ❌        | ❌        | ✅ 核心   | ❌        | ❌        | ❌        |
| Session Replay| ❌        | ❌        | ❌        | ✅ 核心   | ❌        | ❌        |
| 开源           | ❌        | ✅        | ✅        | ❌        | ❌        | ❌        |
| 自托管         | ❌        | ✅ Docker | ✅ pip    | ❌        | ❌        | ❌        |
| 零代码集成     | ❌        | ❌        | ❌        | ❌        | ✅ 代理   | ❌        |
| 多框架支持     | LangChain | ✅ 广泛   | ✅ OTEL  | CrewAI    | ✅ 代理   | ✅        |

## 9. 定价对比（2026）

<!-- version-check: 可观测性平台定价, checked 2026-05-13 -->

```
LangSmith:
├─ 免费：5K traces/月
├─ Plus：$39/月，50K traces
└─ Enterprise：联系销售

LangFuse:
├─ 开源自托管：免费
├─ Cloud 免费：50K observations/月
└─ Pro：$59/月

Arize Phoenix:
├─ 开源：完全免费
└─ Cloud：按用量计费

AgentOps:
├─ 免费：1K sessions/月
└─ Pro：$49/月

Helicone:
├─ 免费：100K requests/月
└─ Pro：按用量计费

Braintrust:
├─ 免费：1K logs/月
└─ Pro：按用量计费
```

## 10. 选型建议

```
选型决策：

用 LangGraph/LangChain？ → LangSmith（最佳集成）
需要自托管？ → LangFuse（开源 Docker 部署）
预算有限？ → Arize Phoenix（完全免费开源）
用 CrewAI？ → AgentOps（原生集成）
不想改代码？ → Helicone（代理模式）
评估驱动开发？ → Braintrust（评估优先）

组合方案：
├─ 小团队：LangFuse 自托管（免费）
├─ 中型团队：LangSmith + LangFuse（互补）
└─ 大型企业：LangSmith Enterprise + Datadog APM
```
## 🎬 推荐视频资源

### 🌐 YouTube
- [LangSmith - Getting Started](https://www.youtube.com/watch?v=tFXm5ijih98) — LangSmith入门教程
- [LangFuse - Open Source LLM Observability](https://www.youtube.com/watch?v=tFXm5ijih98) — LangFuse开源可观测性

### 📖 官方文档
- [LangSmith Docs](https://docs.smith.langchain.com/) — LangSmith文档
- [LangFuse Docs](https://langfuse.com/docs) — LangFuse文档
