# 阿里云百炼与通义
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

## 1. 百炼平台概述

阿里云百炼是一站式 AI Agent 构建平台，提供模型调用、知识库管理、插件市场、工作流编排等能力。支持通义千问全系列模型，也可接入第三方模型。

```
┌──────────────────── 百炼平台 ────────────────────┐
│                                                    │
│  ┌──────────┐  ┌──────────┐  ┌────────────────┐  │
│  │ 模型服务  │  │ 知识库    │  │ 插件/工具      │  │
│  │ Qwen系列  │  │ 文档解析  │  │ 内置+自定义    │  │
│  │ 第三方模型 │  │ 向量检索  │  │ API 插件      │  │
│  └──────────┘  └──────────┘  └────────────────┘  │
│  ┌──────────────────────────────────────────────┐ │
│  │           工作流编排（可视化 + API）            │ │
│  └──────────────────────────────────────────────┘ │
│  ┌──────────────────────────────────────────────┐ │
│  │     DashScope API / OpenAI 兼容接口           │ │
│  └──────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────┘
```

## 2. 通义千问模型系列

| 模型 | 特点 | 适用场景 |
|------|------|---------|
| Qwen-Max | 最强推理能力 | 复杂分析、代码生成 |
| Qwen-Plus | 性价比均衡 | 通用对话、Agent |
| Qwen-Turbo | 速度最快 | 简单任务、高并发 |
| Qwen2.5-72B | 开源最强 | 私有化部署 |
| Qwen-VL | 视觉理解 | 图片分析、OCR |
| Qwen-Audio | 语音理解 | 语音交互 |

## 3. DashScope API 调用

```python
from openai import OpenAI

# 百炼兼容 OpenAI 接口
client = OpenAI(
    api_key="sk-xxx",
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
)

response = client.chat.completions.create(
    model="qwen-max",
    messages=[
        {"role": "system", "content": "你是一个有帮助的助手。"},
        {"role": "user", "content": "解释什么是 AI Agent"},
    ],
    temperature=0.7,
)
print(response.choices[0].message.content)
```

## 4. Function Calling

```python
import json
from openai import OpenAI

client = OpenAI(
    api_key="sk-xxx",
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
)

tools = [
    {
        "type": "function",
        "function": {
            "name": "get_stock_price",
            "description": "查询股票实时价格",
            "parameters": {
                "type": "object",
                "properties": {
                    "symbol": {"type": "string", "description": "股票代码，如 600519"},
                    "market": {"type": "string", "enum": ["sh", "sz"], "description": "交易所"},
                },
                "required": ["symbol"],
            },
        },
    }
]

response = client.chat.completions.create(
    model="qwen-max",
    messages=[{"role": "user", "content": "查一下茅台的股价"}],
    tools=tools,
    tool_choice="auto",
)

# 处理工具调用
message = response.choices[0].message
if message.tool_calls:
    for call in message.tool_calls:
        args = json.loads(call.function.arguments)
        print(f"调用: {call.function.name}, 参数: {args}")
        # → 调用: get_stock_price, 参数: {"symbol": "600519", "market": "sh"}
```

## 5. 百炼 Agent 构建

### 知识库 RAG

```python
# 通过 DashScope API 使用百炼知识库
response = client.chat.completions.create(
    model="qwen-max",
    messages=[{"role": "user", "content": "公司的退货政策是什么？"}],
    extra_body={
        "knowledge_base": {
            "knowledge_base_id": "kb-xxx",
            "top_k": 5,
            "score_threshold": 0.5,
        }
    },
)
```

### 工作流 Agent

```python
# 百炼工作流 API 调用
import dashscope

response = dashscope.Application.call(
    app_id="app-xxx",  # 百炼控制台创建的应用 ID
    prompt="帮我分析上个月的销售数据",
    session_id="session-001",  # 多轮对话
)
print(response.output.text)
```

## 6. MCP 支持

```python
# 通义千问 + MCP Server
from openai import OpenAI
import subprocess, json

# 启动 MCP Server
mcp_process = subprocess.Popen(
    ["npx", "-y", "@modelcontextprotocol/server-filesystem", "/data"],
    stdin=subprocess.PIPE, stdout=subprocess.PIPE,
)

# 百炼也支持在控制台配置 MCP Server
# 控制台 → 应用中心 → 工具管理 → 添加 MCP 工具
```

## 7. 与 LangChain / LlamaIndex 集成

```python
# LangChain 集成
from langchain_community.llms import Tongyi

llm = Tongyi(model="qwen-max", dashscope_api_key="sk-xxx")
result = llm.invoke("什么是 RAG？")

# LlamaIndex 集成
from llama_index.llms.dashscope import DashScope

llm = DashScope(model_name="qwen-max", api_key="sk-xxx")
response = llm.complete("解释向量数据库的原理")
```

## 8. 私有化部署

```bash
# 使用 vLLM 部署 Qwen2.5-72B
pip install vllm
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-72B-Instruct \
    --tensor-parallel-size 4 \
    --max-model-len 32768 \
    --port 8000

# 使用 Ollama 部署（轻量）
ollama pull qwen2.5:72b
ollama serve
```

## 9. 成本对比

| 模型 | 输入价格（每百万 Token） | 输出价格（每百万 Token） |
|------|----------------------|----------------------|
| Qwen-Max | ¥20 | ¥60 |
| Qwen-Plus | ¥4 | ¥12 |
| Qwen-Turbo | ¥2 | ¥6 |
| GPT-4o（对比） | ~¥18 | ~¥72 |
| Claude 3.5 Sonnet（对比） | ~¥21 | ~¥105 |

> 通义千问在中文场景下性价比突出，Qwen-Plus 适合大多数 Agent 场景。

## 10. Qwen3/3.5/3.6 版本演进

> 🔄 更新于 2026-04-21

<!-- version-check: Qwen3.6-Max-Preview, Qwen3.5-397B, checked 2026-04-21 -->

### Qwen3 系列（2025-04-29）

Qwen3 是阿里通义千问的第三代大模型系列，引入混合推理（Hybrid Reasoning）和 MoE 架构：

- 训练数据：36T Token
- 上下文窗口：MoE 模型最高 131K Token，Dense 模型 32K Token
- 核心能力：用户可在深度思考和快速响应之间切换
- 关键模型：Qwen3-235B-A22B（235B 参数 / 22B 激活）、Qwen3-30B-A3B 等
- 来源：[Alibaba Cloud Blog](https://www.alibabacloud.com/blog/602192)、[TechCrunch](https://techcrunch.com/2025/04/28/alibaba-unveils-qwen-3-a-family-of-hybrid-ai-reasoning-models/)
<!-- 修复于 2026-05-20: Qwen3 实际发布于 2025-04-28/29，非 2026-04；上下文窗口 1M 是 Qwen3.6-Plus 的特性，非 Qwen3 系列。已校准发布时间和参数表 -->

### Qwen3.5 系列（2026-02-16）

| 模型 | 总参数 | 激活参数 | GPQA Diamond | 输入价格 |
|------|--------|---------|-------------|---------|
| Qwen3.5-397B-A17B | 397B | 17B | 88.4 | $0.60/M |
| Qwen3.5-Plus | 闭源 | - | - | 按量计费 |

关键特性：MoE 架构激活内存减少 95%，解码速度提升 19x，面向 Agentic 工作流和多模态任务。来源：[LLM Rumors](https://www.llmrumors.com/news/qwen35-alibaba-data-moat-distillation-absent)

### Qwen3.6 系列（2026-04-16 起）

| 模型 | 类型 | 特点 |
|------|------|------|
| Qwen3.6-35B-A3B | 开源（Apache 2.0） | 35B 总参数 / 3B 激活，SWE-Bench 73.4%，多模态 |
| Qwen3.6-Plus | 闭源 | Agentic Coding + 多模态推理，1M 上下文（hybrid MoE） |
| Qwen3.6-Max-Preview | 闭源 | 最强旗舰（~1T 参数 sparse MoE，262K 上下文，6 大 coding benchmark 第一）<!-- 修复于 2026-05-20: 补充参数规模、上下文窗口、benchmark 排名 --> |

战略变化：阿里从纯开源策略转向开源 + 闭源商业化并行，三天内连续发布三个闭源模型。来源：[Alibaba Cloud Model Studio](https://www.alibabacloud.com/help/en/model-studio/newly-released-models)

### 更新后的成本对比

| 模型 | 输入价格（每百万 Token） | 输出价格（每百万 Token） | 备注 |
|------|----------------------|----------------------|------|
| Qwen3.5-397B-A17B | ~¥4.3（$0.60） | - | MoE，性价比极高 |
| Qwen3.6-Plus | 按量计费 | 按量计费 | 闭源旗舰 |
| Qwen-Max（旧） | ¥20 | ¥60 | 逐步被 3.x 替代 |
| Claude Opus 4.6（对比） | ~¥36（$5） | ~¥180（$25） | 约 8x 贵于 Qwen3.5 |
## 🎬 推荐视频资源

### 📺 B站
- [阿里云百炼平台教程](https://www.bilibili.com/video/BV1Bz421B7bG) — 百炼平台中文教程
- [通义千问API使用教程](https://www.bilibili.com/video/BV1PD421E7mN) — 通义千问实战

### 📖 官方文档
- [阿里云百炼](https://bailian.console.aliyun.com/) — 百炼平台
- [通义千问API](https://help.aliyun.com/zh/model-studio/) — 通义千问文档
