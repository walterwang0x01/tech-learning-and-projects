# 代码质量工具
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

> Python 代码质量检查和格式化工具

## 1. 代码格式化

### 1.1 Black

```bash
pip install black
```

```bash
# 格式化文件
black script.py

# 格式化目录
black src/

# 检查格式（不修改）
black --check src/
```

### 1.2 autopep8

```bash
pip install autopep8
```

```bash
# 格式化文件
autopep8 --in-place script.py

# 格式化目录
autopep8 --in-place --recursive src/
```

## 2. 代码检查

### 2.1 Flake8

```bash
pip install flake8
```

```bash
# 检查代码
flake8 script.py

# 忽略特定错误
flake8 --ignore=E501,W503 script.py
```

### 2.2 Pylint

```bash
pip install pylint
```

```bash
# 检查代码
pylint script.py

# 生成报告
pylint --output-format=html script.py > report.html
```

## 3. 类型检查

### 3.1 mypy

```bash
pip install mypy
```

```python
# 添加类型注解
def add(a: int, b: int) -> int:
    return a + b
```

```bash
# 类型检查
mypy script.py
```

## 4. 代码复杂度

### 4.1 Radon

```bash
pip install radon
```

```bash
# 计算复杂度
radon cc script.py

# 计算圈复杂度
radon cc -a script.py
```

## 5. 安全扫描

### 5.1 Bandit

```bash
pip install bandit
```

```bash
# 安全扫描
bandit -r src/

# 生成报告
bandit -r src/ -f json -o report.json
```

## 6. 代码覆盖率

### 6.1 Coverage.py

```bash
pip install coverage
```

```bash
# 运行测试并收集覆盖率
coverage run -m pytest

# 生成报告
coverage report
coverage html
```

## 7. 集成工具

### 7.1 pre-commit

```bash
pip install pre-commit
```

```yaml
# .pre-commit-config.yaml
repos:
  - repo: https://github.com/psf/black
    rev: 23.3.0
    hooks:
      - id: black
  - repo: https://github.com/pycqa/flake8
    rev: 6.0.0
    hooks:
      - id: flake8
```

```bash
# 安装钩子
pre-commit install

# 手动运行
pre-commit run --all-files
```

## 8. 总结

代码质量工具要点：
- **格式化**：Black、autopep8
- **代码检查**：Flake8、Pylint
- **类型检查**：mypy
- **复杂度分析**：Radon
- **安全扫描**：Bandit
- **覆盖率**：Coverage.py
- **集成**：pre-commit

使用这些工具可以保持代码质量和一致性。

> 🔄 更新于 2026-04-18

<!-- version-check: Ruff 0.15.x, checked 2026-04-18 -->

## 9. Ruff — 2026 年推荐的 Python Linter 和 Formatter

[Ruff](https://docs.astral.sh/ruff/) 是 Astral 用 Rust 编写的极速 Python linter 和代码格式化工具。2026 年已在大量项目中替代 Flake8 + Black + isort 的组合，成为 Python 代码质量工具的首选。

### 9.1 核心优势

- **极速**：比 Flake8 快 10-100 倍
- **All-in-One**：同时替代 Flake8（lint）+ Black（format）+ isort（import 排序）
- **800+ 规则**：覆盖 Flake8、pylint、pyupgrade、isort 等多个工具的规则
- **100% Black 兼容**：格式化输出与 Black 一致
- **GitHub Stars**：42.5K+

### 9.2 安装和使用

```bash
# 安装（推荐使用 uv）
uv add --dev ruff

# 或使用 pip
pip install ruff

# 代码检查（lint）
ruff check .

# 自动修复
ruff check --fix .

# 代码格式化（替代 Black）
ruff format .

# 检查格式（不修改）
ruff format --check .

# import 排序（替代 isort）
ruff check --select I --fix .
```

### 9.3 配置（pyproject.toml）

```toml
[tool.ruff]
# 目标 Python 版本
target-version = "py314"
# 行长度（与 Black 一致）
line-length = 88

[tool.ruff.lint]
# 启用的规则集
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort（import 排序）
    "N",    # pep8-naming
    "UP",   # pyupgrade（自动升级旧语法）
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
]
ignore = ["E501"]  # 忽略行长度（由 formatter 处理）

[tool.ruff.format]
# 使用双引号（与 Black 一致）
quote-style = "double"
# 缩进使用空格
indent-style = "space"
```

### 9.4 Ruff vs 传统工具对比

| 特性 | Ruff | Flake8 + Black + isort |
|------|------|----------------------|
| 速度 | ⚡ 极快（Rust） | 慢（Python） |
| 工具数量 | 1 个 | 3+ 个 |
| 配置文件 | 1 个（pyproject.toml） | 3+ 个 |
| 自动修复 | ✅ 大量规则支持 | 部分支持 |
| 格式化 | ✅ 内置（Black 兼容） | 需要 Black |
| import 排序 | ✅ 内置 | 需要 isort |

### 9.5 ty 类型检查器（Astral，Beta）

> 🔄 更新于 2026-05-21

<!-- version-check: ty Beta (Astral), checked 2026-05-21 -->

Astral（uv 和 Ruff 的开发团队）发布了 ty —— 一个用 Rust 编写的极速 Python 类型检查器和语言服务器。

```bash
# 安装
uv tool install ty@latest

# 或使用 pip
pip install ty

# 类型检查
ty check

# 指定目录
ty check src/

# 监听文件变化（语言服务器模式）
ty server
```

**核心特性**：

- **极速**：比 Pyright 快 10-100x，比 mypy 快 10-60x（无缓存）
- **增量分析**：细粒度增量计算，编辑文件后近乎即时反馈
- **语言服务器**：完整 LSP 实现（代码导航、补全、自动导入、重命名、inlay hints、hover、签名帮助、快速修复、语义高亮）
- **渐进式类型**：最小化误报，尊重渐进类型系统
- **VS Code 扩展**：官方扩展，实时类型检查

**配置**（`pyproject.toml`）：

```toml
[tool.ty]
# 规则级别配置
[tool.ty.rules]
# 可配置每条规则的严重级别
possibly-unbound-attribute = "warn"
invalid-return-type = "error"

[tool.ty.environment]
# Python 版本
python-version = "3.14"
```

**性能基准**（home-assistant 项目，M4 芯片，无缓存）：

| 工具 | 时间 | 相对速度 |
|------|------|---------|
| ty | 4.7ms | 1x（基准） |
| Pyright | 386ms | 82x 慢 |
| Pyrefly | 2.38s | 506x 慢 |
| mypy | ~数秒 | 数百倍慢 |

**ty vs mypy vs Pyright 选型**：

| 维度 | ty | mypy | Pyright |
|------|-----|------|---------|
| 速度 | ⚡⚡⚡ 极快 | 🐢 慢 | ⚡ 快 |
| 语言服务器 | ✅ 内置 | ❌ 无 | ✅ Pylance |
| 生态成熟度 | Beta | 稳定 | 稳定 |
| 增量检查 | ✅ 细粒度 | ✅ 粗粒度 | ✅ 中等 |
| 配置 | pyproject.toml | mypy.ini / pyproject.toml | pyrightconfig.json |
| 插件系统 | ❌ 暂无 | ✅ 丰富 | ❌ 无 |

**当前状态**：Beta 阶段，快速迭代中。适合尝鲜和 CI 加速校验，生产项目建议与 mypy/Pyright 并行使用。

来源：[Astral 官方博客](https://astral.sh/blog/ty)、[PyPI](https://pypi.org/project/ty/)、[GitHub](https://github.com/astral-sh/ty)

### 9.6 2026 年推荐工具链

```
uv（包管理）+ ruff（lint + format）+ mypy 或 ty（类型检查）
```

两个工具 + 一个配置文件（`pyproject.toml`），覆盖 Python 项目的全部代码质量需求。

**选型建议**：
- 新项目 + 追求速度 → ty（Beta，但已可用于 CI）
- 生产项目 + 稳定性优先 → mypy（成熟生态，丰富插件）
- VS Code 用户 + 实时反馈 → ty 语言服务器 或 Pylance（Pyright）

> 来源：[Ruff 官方文档](https://docs.astral.sh/ruff/)、[ty 官方文档](https://docs.astral.sh/ty/)、[GitHub](https://github.com/astral-sh/ruff)

