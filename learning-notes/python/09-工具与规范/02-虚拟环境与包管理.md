# 虚拟环境与包管理
‍‍​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌​​​​​​​​​​​‌‌‌​‌​​​​​​​​​​​‌‌​​‌​‌​​​​​​​​​‌‌‌​​‌​​​​​​​​​​​‌​​​​​​​​​​​​​​‌​‌​‌‌‌​​​​​​​​​‌‌​​​​‌​​​​​​​​​‌‌​‌‌‌​​​​​​​​​​‌‌​​‌‌‌‍‍
> Author: Walter Wang

> Python 虚拟环境和包管理的最佳实践

## 1. 虚拟环境

### 1.1 venv（标准库）

```bash
# 创建虚拟环境
python -m venv venv

# 激活（Windows）
venv\Scripts\activate

# 激活（Linux/Mac）
source venv/bin/activate

# 停用
deactivate
```

### 1.2 virtualenv

```bash
# 安装
pip install virtualenv

# 创建
virtualenv venv

# 指定Python版本
virtualenv -p python3.10 venv
```

### 1.3 conda

```bash
# 创建环境
conda create -n myenv python=3.10

# 激活
conda activate myenv

# 安装包
conda install numpy pandas

# 导出环境
conda env export > environment.yml
```

## 2. 包管理工具

### 2.1 pip

```bash
# 安装包
pip install package_name

# 安装特定版本
pip install package_name==1.0.0

# 从requirements.txt安装
pip install -r requirements.txt

# 生成requirements.txt
pip freeze > requirements.txt

# 升级包
pip install --upgrade package_name
```

### 2.2 Poetry

```bash
# 安装
pip install poetry

# 初始化项目
poetry init

# 添加依赖
poetry add fastapi uvicorn

# 安装依赖
poetry install

# 更新依赖
poetry update

# 导出requirements.txt
poetry export -f requirements.txt --output requirements.txt
```

### 2.3 pipenv

```bash
# 安装
pip install pipenv

# 创建环境并安装
pipenv install

# 安装开发依赖
pipenv install --dev pytest

# 激活shell
pipenv shell

# 生成requirements.txt
pipenv requirements > requirements.txt
```

## 3. 依赖管理

### 3.1 requirements.txt

```txt
# 精确版本
fastapi==0.104.1
uvicorn==0.24.0

# 版本范围
pydantic>=1.10.0,<2.0.0

# Git依赖
git+https://github.com/user/repo.git@branch

# 本地依赖
./local-package
```

### 3.2 setup.py

```python
from setuptools import setup, find_packages

setup(
    name="myproject",
    version="1.0.0",
    install_requires=[
        "fastapi>=0.100.0",
        "uvicorn>=0.20.0",
    ],
    extras_require={
        "dev": ["pytest", "black", "mypy"],
    },
    packages=find_packages(),
)
```

## 4. 最佳实践

### 4.1 项目结构

```
project/
├── venv/              # 虚拟环境（不提交）
├── requirements.txt    # 生产依赖
├── requirements-dev.txt # 开发依赖
├── setup.py           # 包配置
└── src/
    └── app/
```

### 4.2 依赖锁定

```bash
# 使用pip-tools
pip install pip-tools

# 生成requirements.in
echo "fastapi" > requirements.in

# 编译锁定版本
pip-compile requirements.in

# 更新
pip-compile --upgrade requirements.in
```

## 5. 总结

虚拟环境和包管理要点：
- **虚拟环境**：隔离项目依赖
- **包管理**：pip、Poetry、pipenv
- **依赖文件**：requirements.txt、setup.py
- **版本锁定**：确保环境一致性
- **最佳实践**：分离生产和开发依赖

良好的依赖管理是项目成功的基础。

> 🔄 更新于 2026-04-18

<!-- version-check: uv 0.11.17 (2026-05-28), checked 2026-05-31 -->

## 6. uv — 2026 年推荐的 Python 项目管理器

[uv](https://docs.astral.sh/uv/) 是 Astral（Ruff 的创建者）用 Rust 编写的极速 Python 包和项目管理器。2026 年已成为新项目的事实标准，统一替代 pip + venv + pyenv + pip-tools 的组合。

### 6.1 核心优势

- **极速**：比 pip 快 10-100 倍
- **统一工具链**：Python 版本管理 + 虚拟环境 + 依赖管理 + 构建发布
- **跨平台锁文件**：`uv.lock` 确保所有平台一致的依赖解析
- **兼容 pip**：支持 `requirements.txt`、`pyproject.toml`
- **GitHub Stars**：83.5K+

> 🔄 更新于 2026-05-31：当前最新稳定版为 **uv 0.11.17**（2026-05-28，[PyPI](https://pypi.org/project/uv/)）。版本演进节奏极快：0.8.0（2025-07）→ 0.9.0（2025-10）→ 0.10.0（2026-02-05）→ 0.11.0（2026-03-23）→ 0.11.17。uv 仍处于 0.x 阶段，[官方版本策略](https://docs.astral.sh/uv/reference/policies/versioning/)说明 minor 版本号（中间位）可能包含行为变化，建议在 CI 中固定 uv 版本（如 `astral-sh/setup-uv` 指定 `version:`），升级前查看 [changelog](https://github.com/astral-sh/uv/releases)。

### 6.2 安装

```bash
# macOS / Linux
curl -LsSf https://astral.sh/uv/install.sh | sh

# 或使用 Homebrew
brew install uv

# 或使用 pip（不推荐，建议独立安装）
pip install uv
```

### 6.3 项目管理

```bash
# 创建新项目（自动创建 pyproject.toml 和虚拟环境）
uv init my-project
cd my-project

# 添加依赖
uv add fastapi uvicorn
uv add --dev pytest ruff

# 安装所有依赖
uv sync

# 运行命令（自动使用虚拟环境）
uv run python main.py
uv run pytest

# 更新依赖
uv lock --upgrade

# 导出 requirements.txt（兼容传统工作流）
uv export --format requirements-txt > requirements.txt
```

### 6.4 Python 版本管理

```bash
# 安装 Python 版本
uv python install 3.14

# 安装 free-threaded 版本
uv python install 3.14t

# 列出已安装版本
uv python list

# 固定项目 Python 版本
uv python pin 3.14
```

### 6.5 pyproject.toml 示例

```toml
[project]
name = "my-project"
version = "0.1.0"
description = "项目描述"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.0",
    "uvicorn>=0.34.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "ruff>=0.15.0",
]

[tool.uv]
# uv 特定配置
dev-dependencies = ["pytest", "ruff"]
```

### 6.6 uv vs 其他工具对比

| 特性 | uv | pip + venv | Poetry | pipenv |
|------|-----|-----------|--------|--------|
| 安装速度 | ⚡ 极快（Rust） | 慢 | 中等 | 慢 |
| Python 版本管理 | ✅ 内置 | ❌ 需 pyenv | ❌ 需 pyenv | ❌ 需 pyenv |
| 锁文件 | ✅ `uv.lock` | ❌ 需 pip-tools | ✅ `poetry.lock` | ✅ `Pipfile.lock` |
| 跨平台锁定 | ✅ | ❌ | ❌ | ❌ |
| 虚拟环境管理 | ✅ 自动 | 手动 | ✅ 自动 | ✅ 自动 |
| pip 兼容 | ✅ `uv pip` | ✅ 原生 | ❌ | ❌ |
| 工具运行 | ✅ `uvx` | ❌ | ❌ | ❌ |

> 来源：[uv 官方文档](https://docs.astral.sh/uv/)、[GitHub](https://github.com/astral-sh/uv)

