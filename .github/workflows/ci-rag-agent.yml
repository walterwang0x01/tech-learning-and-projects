name: RAG Agent CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'projects/rag-llm-agent-platform/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'projects/rag-llm-agent-platform/**'

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: pgvector/pgvector:pg15
        env:
          POSTGRES_DB: rag_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'
        cache: 'pip'
    
    - name: Install dependencies
      working-directory: ./projects/rag-llm-agent-platform
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest pytest-asyncio pytest-cov
    
    - name: Run tests
      working-directory: ./projects/rag-llm-agent-platform
      run: |
        pytest tests/ -v --cov=app --cov-report=xml --cov-report=term
    
    - name: Upload coverage to Codecov
      if: always()
      uses: codecov/codecov-action@v3
      with:
        file: ./projects/rag-llm-agent-platform/coverage.xml
        flags: rag-agent
        name: rag-agent-coverage
    
    - name: Lint with flake8
      working-directory: ./projects/rag-llm-agent-platform
      run: |
        pip install flake8
        flake8 app/ --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 app/ --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    
    - name: Build Docker image
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      working-directory: ./projects/rag-llm-agent-platform
      run: |
        docker build -t rag-agent-platform:latest -f Dockerfile .

