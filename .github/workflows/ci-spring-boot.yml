name: Spring Boot CI

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'projects/spring-boot-microservice-demo/**'
  pull_request:
    branches: [ main, develop ]
    paths:
      - 'projects/spring-boot-microservice-demo/**'

jobs:
  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_DB: microservice_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      kafka:
        image: confluentinc/cp-kafka:7.5.0
        env:
          KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
        ports:
          - 9092:9092
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    
    - name: Run tests
      working-directory: ./projects/spring-boot-microservice-demo
      run: mvn clean test
    
    - name: Generate test report
      if: always()
      uses: dorny/test-reporter@v1
      with:
        name: Maven Tests
        path: projects/spring-boot-microservice-demo/**/target/surefire-reports/*.xml
        reporter: java-junit
    
    - name: Build
      working-directory: ./projects/spring-boot-microservice-demo
      run: mvn clean package -DskipTests
    
    - name: Build Docker image
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      working-directory: ./projects/spring-boot-microservice-demo
      run: |
        docker build -t user-service:latest -f Dockerfile .
    
    - name: Check code coverage
      working-directory: ./projects/spring-boot-microservice-demo
      run: |
        mvn jacoco:report
        # 可以添加覆盖率检查逻辑

