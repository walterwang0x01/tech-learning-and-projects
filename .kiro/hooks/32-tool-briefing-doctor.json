{
  "version": "v1",
  "hooks": [
    {
      "name": "🩺 简报索引一致性检查",
      "description": "扫描简报 md 与 published-index.json 的一致性，发现 missing / hash_drift / orphan 三类问题并自动修复（旧格式 md 走 backfill 路径只补 file_hash 不动 items）。",
      "trigger": "Manual",
      "action": {
        "type": "command",
        "command": "python3 scripts/briefing-tools.py doctor --fix"
      },
      "enabled": true
    }
  ]
}
